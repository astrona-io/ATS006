# lab-021: chmod Permission Bits Lab

QEMU VM for the LFCS course — configuring setgid group inheritance, an owner-only script lockdown, and the sticky bit on a shared drop directory.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-021
```
