# lab-051: Archive Conversion & Verification Lab

QEMU VM for the LFCS course — converting a bzip2 archive to a maximally-compressed gzip archive without touching the original, then proving equivalence with sorted content listings.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-051
```
