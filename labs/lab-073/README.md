# lab-073: SSL Certificate Generation Lab

QEMU VM for the LFCS course — generating an RSA key, a SAN-bearing self-signed certificate, and a CSR with openssl, then verifying the key/certificate pairing.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-073
```
