# lab-041: Git Fundamentals Lab

QEMU VM for the LFCS course — initializing a repository from scratch, staging and committing isolated changes, writing a `.gitignore`, and wiring up a local bare repository as a stand-in remote.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-041
```
