# lab-070: Service Configuration Capstone Lab

QEMU VM for the LFCS course — wrapping a script as a systemd service, safely overriding one of its runtime parameters with a systemctl edit drop-in, and issuing it a self-signed TLS key/certificate pair.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-070
```
