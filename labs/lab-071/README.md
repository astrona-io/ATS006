# lab-071: systemd Unit Creation Lab

QEMU VM for the LFCS course — wrapping an unmanaged script as a properly supervised, boot-persistent systemd service.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-071
```
