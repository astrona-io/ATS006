# lab-042: Git Branch Inspection & Merge Lab

QEMU VM for the LFCS course — cloning a repository, inspecting a config file across three candidate branches without checking any of them out, merging only the correct one, and committing a directory the `.keep` way.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-042
```
