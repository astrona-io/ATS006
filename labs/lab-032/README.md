# lab-032: Shell History Recall Lab

QEMU VM for the LFCS course — persisting HISTSIZE/HISTFILESIZE/HISTCONTROL/HISTTIMEFORMAT behavior and reconstructing a prior command sequence from shell history.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-032
```
