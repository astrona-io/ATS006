# lab-050: Archiving & Backup Capstone Lab

QEMU VM for the LFCS course — converting a bzip2 report archive to verified gzip, and separately running a full-then-incremental `tar` backup of a live service directory with a proven restore.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-050
```
