# lab-072: systemd Unit Override Lab

QEMU VM for the LFCS course — safely overriding a vendor-packaged nginx.service with a systemctl edit drop-in instead of editing the shipped unit file.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-072
```
