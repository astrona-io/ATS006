# lab-033: Command Aliases Lab

QEMU VM for the LFCS course — persisting safe aliases, confirming what each name resolves to, and bypassing a safety alias for exactly one invocation.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-033
```
