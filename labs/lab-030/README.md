# lab-030: Everyday Shell Craft Capstone Lab

QEMU VM for the LFCS course — mining and redacting incident logs with grep/sed, reconstructing a prior command sequence from shell history, and persisting safe aliases, combined into one handoff scenario.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-030
```
