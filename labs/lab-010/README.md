# lab-010: Shell Semantics Integration Capstone

QEMU VM for the LFCS course — building a monitoring wrapper script that exports the right variable to a child health-check process while keeping another variable shell-local, and correctly redirects and captures the child's stdout, stderr, and exit code.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-010
```
