# lab-011: Shell Redirection & Exit Code Diagnostics

QEMU VM for the LFCS course — redirecting stdout and stderr independently, combining both streams correctly, and capturing a program's numeric exit status.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-011
```
