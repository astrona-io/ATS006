# lab-020: Permissions & File Triage Capstone Lab

QEMU VM for the LFCS course — setting a persistent per-user umask, applying setgid/owner-lockdown/sticky-bit chmod across a shared team workspace, and triaging a legacy incoming directory with a multi-stage `find` cleanup.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-020
```
