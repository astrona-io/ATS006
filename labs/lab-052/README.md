# lab-052: tar Backup Strategy Lab

QEMU VM for the LFCS course — building a full `tar` backup that preserves permissions and excludes a cache directory, capturing a true incremental backup of only what changed, and proving both restores are correct.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-052
```
