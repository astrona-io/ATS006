# lab-031: grep & sed Text Processing Lab

QEMU VM for the LFCS course — extracting log lines that satisfy multiple simultaneous grep conditions and redacting whole lines matching a shape with sed.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-031
```
