# lab-040: Git Operations Capstone Lab

QEMU VM for the LFCS course — cloning a shared deploy-configs repository, merging the correct feature-flag branch, committing a directory the `.keep` way, and reconciling a topic branch against a simulated teammate's upstream commit with a rebase.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-040
```
