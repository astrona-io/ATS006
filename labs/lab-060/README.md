# lab-060: Diskspace & Sync Capstone Lab

QEMU VM for the LFCS course — reclaiming space from a deleted-but-open file, then building a space-efficient local `rsync --link-dest` snapshot rotation of the recovered directory.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-060
```
