# lab-023: find Triage by Criteria Lab

QEMU VM for the LFCS course — a multi-stage `find` cleanup: delete files by absolute age cutoff, then triage the remainder by size and permission into destination folders.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS006.git -c labs/lab-023
```
